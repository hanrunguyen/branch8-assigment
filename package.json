{
  "name": "branch8-fronend-dahostel",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "npm run build && git add dist -f && git commit -m 'deploy' && git subtree push --prefix dist origin gh-pages",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.49.8",
    "vite": "^2.8.0"
  },
  "dependencies": {
    "jquery": "^3.6.0",
    "slick-carousel": "^1.8.1"
  }
}
